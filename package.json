{
  "name": "iconcaptcha-solver",
  "version": "0.2.0",
  "description": "",
  "main": "index.node",
  "scripts": {
    "test": "cargo test",
    "cargo-build": "cargo build --message-format=json-render-diagnostics > cargo.log",
    "cross-build": "cross build --message-format=json-render-diagnostics > cross.log",
    "postcargo-build": "neon dist < cargo.log",
    "postcross-build": "neon dist -m /target < cross.log",
    "debug": "npm run cargo-build --",
    "build": "npm run cargo-build -- --release",
    "cross": "npm run cross-build -- --release"
  },
  "author": "moz4rtdev",
  "license": "ISC",
  "devDependencies": {
    "@neon-rs/cli": "0.1.82"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/moz4rtdev/iconcaptcha-solver-rs.git"
  },
  "bugs": {
    "url": "https://github.com/moz4rtdev/iconcaptcha-solver-rs/issues"
  },
  "homepage": "https://github.com/moz4rtdev/iconcaptcha-solver-rs#readme"
}
